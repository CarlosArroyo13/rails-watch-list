<div class="container shadow p-3 mt-5 w-75">
  <div class="d-flex justify-content-between">
    <h1 class="fw-bold">Lista de peliculas</h1>
    <%= link_to "Add a new list", new_list_path, class: "btn btn-primary rounded-5 h-50" %>
  </div>
  <% @lists.each do |list| %>
    <hr>
    <p><%= link_to list.name, list_path(list), class: "text-decoration-none" %></p>
  <% end %>
  <hr>
</div>
