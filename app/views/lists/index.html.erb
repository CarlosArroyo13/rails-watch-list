<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://www.shutterstock.com/image-photo/happy-family-spending-time-together-260nw-2529762817.jpg);">
    <div class="container">
      <h1><strong> 📽️Luces, Cámara, Sofá </strong></h1>
      <p>Encuentra las mejores playlist con tan solo un clic!.</p>
    </div>
</div>


<div class="container shadow p-3 mt-5 w-75 bg-light">
  <div class="d-flex justify-content-between">
    <h1 class="fw-bold">Lista de peliculas</h1>
    <%= link_to "Add a new list", new_list_path, class: "btn btn-primary rounded-5 h-50" %>
  </div>
    <div class="cards">
      <% @lists.each do |list| %>
            <div class="card-category text-center"
              style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path list.photo.key, crop: :fill %>')">
              <%= link_to list.name, list_path(list), class: " fs-4 text-decoration-none text-white" %>
            </div>
      <% end %>
    </div>
</div>
